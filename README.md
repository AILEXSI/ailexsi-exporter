# ailexsi-exporter
AILEXSI Exporter — Standalone blueprint for rendering project timelines to MP4 (and later other formats). Consumes timeline + media, produces final video. Local-first, FFmpeg / native backend ready.
